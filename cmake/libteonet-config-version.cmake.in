set (PACKAGE_VERSION @BSON_VERSION@)

if ("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@TEONET_MAJOR_VERSION@")
   if ("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@TEONET_MINOR_VERSION@")
      set (PACKAGE_VERSION_EXACT TRUE)
   elseif ("${PACKAGE_FIND_VERSION_MINOR}" LESS "@TEONET_MINOR_VERSION@")
      set (PACKAGE_VERSION_COMPATIBLE TRUE)
   else ()
      set (PACKAGE_VERSION_UNSUITABLE TRUE)
   endif ()
elseif (PACKAGE_FIND_VERSION)
   set (PACKAGE_VERSION_UNSUITABLE TRUE)
endif ()
